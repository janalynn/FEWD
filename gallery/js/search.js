Element.prototype.Search = function(){
  var gallery= document.getElementById('gallery');
  gallery.getFilteredLis()
  // when the user focuses (clicks) on the input, clear its contents
  // after the user presses 'Enter/Return', filter the gallery <li> using tags from the JSON model.
    //example -- li.dataset.description = photo.description; use data-tags
};
// I don't know... :(
var search = this

li.innerHTML = '<div class="meta"><h5>'+
      photo.name+
      '</h5><h6>'+
      photo.user.fullname+
      '<h6></div><div class="stats"><div>'+
      photo.rating+'</div></div>'+
      '</div>';
li.addEventListener('mousedown',gallery.singlePhoto);
ul.appendChild(li);

var something = function (){

};
